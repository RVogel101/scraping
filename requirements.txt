# Requirements for CWAS Processing Pipeline
# Core dependencies for image processing and data handling

# Image processing
Pillow>=9.0.0

# Data handling  
pandas>=1.3.0

# Web scraping (already required by scraper)
selenium>=4.0.0
requests>=2.25.0

# Standalone preview API
fastapi>=0.112.0
uvicorn>=0.30.0

# System monitoring (optional, for performance tracking)
psutil>=5.8.0

# Note: Make sure Chrome/Chromium and ChromeDriver are installed separately